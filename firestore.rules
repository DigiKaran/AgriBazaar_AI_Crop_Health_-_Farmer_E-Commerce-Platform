rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents/{document=**} {
    // For this college project, allow any authenticated user to read and write all data.
    // This removes all complex security rules to ensure the application works without permission errors.
    allow read, write: if request.auth != null;
  }
}
