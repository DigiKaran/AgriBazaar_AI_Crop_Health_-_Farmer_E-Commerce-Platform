
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: These rules are insecure and allow public access.
    // They are intended for local development and demonstration ONLY for a college project.
    // Do not use these rules in a production environment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
